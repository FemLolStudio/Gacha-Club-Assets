package
{
   import flash.display.BitmapData;
   
   public dynamic class shadowmeguna extends BitmapData
   {
       
      
      public function shadowmeguna(w:int = 596, h:int = 600)
      {
         super(w,h);
      }
   }
}
